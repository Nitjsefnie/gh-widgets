# Deny-by-default. `*` ignores everything; each rule below names back exactly
# what gh-widgets ships. An unlisted file is invisible to git and can never be
# committed by accident — which is the point, because this working tree also
# holds .secrecy-literals, a .venv, .worktrees/ and the rendered widgets/
# output.
#
# The mechanic, because the pattern fails silently without it: git does not
# look inside a directory it has already ignored. So every kept directory is
# re-opened (`!dir/`), its contents denied again (`dir/*`), and its files are
# named back by a glob SCOPED to that directory. That scoping is the point —
# it is what stops a `!*.py` from reaching into `__pycache__/`, `.venv/` or
# the generated `widgets/` tree.
#
# NOTE the shape of this repo: the renderers and their tests live at the
# ROOT, not under a package directory. So the root block below names back
# `*.py` directly — and that is exactly why `__pycache__/` must stay denied
# by the leading `*` and never re-opened.
#
# Adding a file whose type is not already named back for its directory means
# adding a rule here. `git status` will NOT tell you the file is missing; it
# simply never appears. `git check-ignore -v <path>` names the rule hiding it.

*

# ---- repository root — renderers, shared modules, tests, config -----------
!/*.py
!/.gitignore
!/.pylintrc
!/CLAUDE.md
!/CONTRIBUTING.md
!/LICENSE
!/PRESENTATION.txt
!/README.md
!/VERSION
!/install.sh
!/pyrightconfig.json
!/requirements-dev.txt
!/requirements-test.txt
!/setup.cfg

# `!/*.py` above matches only files directly at the root, so __pycache__/
# stays denied. Re-stated explicitly because this is the one repo where the
# root glob is broad enough for that to matter.
/__pycache__/

# ---- .github/ — CI workflows and dependabot -------------------------------
!/.github/
/.github/*
!/.github/dependabot.yml
!/.github/workflows/
/.github/workflows/*
!/.github/workflows/*.yml

# ---- docs/ — design docs and specs -----------------------------------------
!/docs/
/docs/*
!/docs/*.html
!/docs/*.md
!/docs/*.txt
!/docs/superpowers/
/docs/superpowers/*
!/docs/superpowers/plans/
/docs/superpowers/plans/*
!/docs/superpowers/plans/*.html
!/docs/superpowers/plans/*.md
!/docs/superpowers/specs/
/docs/superpowers/specs/*
!/docs/superpowers/specs/*.html
!/docs/superpowers/specs/*.md

# ---- examples/ — sample unit, timer and nginx config -----------------------
!/examples/
/examples/*
!/examples/*.conf
!/examples/*.service
!/examples/*.timer

# ---- scripts/ — repo tooling ------------------------------------------------
!/scripts/
/scripts/*
!/scripts/*.py
!/scripts/*.sh
!/scripts/pre-push
!/scripts/ci/
/scripts/ci/*
!/scripts/ci/*.py

# ---- units/ — the systemd units actually deployed ---------------------------
!/units/
/units/*
!/units/*.service
!/units/*.timer
